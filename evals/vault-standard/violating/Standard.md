---
type: standard
status: current
updated: 2026-08-18
---

# Standard (fixture, deliberately non-conformant)

No `standard-version` in the frontmatter above, so a stale copy cannot be told from a current one.

## Profile

Adds the note types `project`, `topic`, `literature`, `lab-entry`, and `lab-entry` notes use the
profile status `recorded`. Only `recorded` is a status; the four note types in the same sentence
declare nothing.
