# Working on alpha (fixture)

A fixture repo used by `evals/fleet-standard/run.mjs`. It stands in for a consenting fleet
member whose standards contract is a byte-identical pair.

## Fleet

fleet member: yes

Alpha is enrolled in the fixture workspace fleet. Doctrine changes arrive through the fleet
mode of `/code-ops-suite:conform` and land as ordinary reviewed commits.

## Documentation

The vault `alpha-docs/` is canonical for design-time judgment. Its `Standard.md` carries the
routing table for new work.
