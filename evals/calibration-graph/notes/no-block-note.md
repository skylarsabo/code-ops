# Calibration note — fixture (no Machine block)

A note produced against an older template version, with every metric written only as
prose. Ingest must fail closed rather than guess: a note with no Machine block carries no
machine-readable run, and silently ingesting an empty skeleton would render an empty row.

## Lessons

The register cap fired again and the extractor parsed nothing.
