# Calibration trend table

**DERIVED — do not hand-edit.** Every row is rendered from the run documents under
`evals/calibration/` by `node scripts/calibration-graph.mjs render`; CI runs
`render --check` and fails on any drift. To change a cell, change the run document.

The trend SSOT for real-scale calibration runs (`/code-ops-suite:calibration-run`),
sibling to `evals/FLOOR_TABLE.md`. A run document is fed **only** by a sanitized
calibration note that has already passed `scripts/calibration-metrics.mjs --validate-note`
— never by a hand-written row, and never with a target repo's name, path, or internals.
See `docs/techniques/calibration-protocol.md` for the one-way channel rule, the run
design, and the note template a run document is ingested from.

| date | suite versions | target label | dispatches | CONFIRMED ratio | refutation survival | notes |
| --- | --- | --- | --- | --- | --- | --- |
| 2026-07-28 | code-ops-suite@1.24.0 | mid-size Python data-measurement service | 9 | 0.68 (19 of 28) | 0.33 (1 of 3 paneled; 2 repro-exempt) | first row; 0 dangling, 0 failed, 0 redispatched; panels killed 2 overstated highs (one proposed fix would have been harmful); accepted-by-design checks must read dated decision records; register exceeded 120-line hard cap; extractor could not parse prose-style register — emit parseable row schema next run |
| 2026-07-28 | code-ops-suite@1.24.0 | mid-size TypeScript SaaS web app (payments, background jobs, public scoring surface) | 16 | 0.71 (35 of 49) | 0.83 (5 of 6 paneled) | 0 dangling, 0 failed, 0 redispatched across ~1.53M operative tokens; assess-only, 0 enforcements added, redaction clean. **Lead model auto-substituted one tier down twice mid-run, both times on security-lens synthesis, never on other lenses; one tool call failed mid-intervention** — an unattended run would finish its security phase and all downstream verdicts a tier low, and model identity appears nowhere in artifacts, so the tier-mix metric is not reconstructable: stamp resolved lead model into the ledger per phase. Panels changed 4 of 6 findings (1 downgrade, 1 re-scope onto a broader path, 1 tier upgrade via executed check, 1 kill) — the kill was the run's headline finding, defeated by 4 mechanisms none of which were in the files it cited; keep panels mandatory at high severity. Assess-only under-tiers itself: operatives cannot execute, so only a panelist's executed check reached CONFIRMED by reproduction — compare CONFIRMED ratios within-track only. **Prior row's two lessons recurred verbatim**: register again over the 120-line hard cap (49 findings → ~2.5x; cap likely wrong for real scale) and extractor again parsed 0 of everything until IDs, tier/severity fields and ledger columns were reshaped — publish the three grammars with a conforming example and warn on zero-items-in-non-empty-file. Redaction gate crashes (uncaught, non-fail-closed exit code) when given a run folder, which is the invocation its own docs imply |
| 2026-07-29 | code-ops-suite@1.25.0 | large TypeScript monorepo — real-time telephony platform with a payment lane, chat-bot operator surface, and infrastructure-as-code | 9 | 0.57 (16 of 28) | 1.00 (1 of 1 paneled; 4 repro-exempt) | 0 dangling, 0 failed, 0 redispatched (third consecutive perfect run) across ~1.1M operative tokens; assess-only, 0 enforcements added, redaction clean. **Two prior-row lessons verifiably landed**: the zero-items-in-non-empty-file warning fired and is what caught this run's parse failure (without it the run would have reported a plausible but wholly wrong metric set), and the redaction gate no longer crashes on a run folder. **New, load-bearing: the item-ID pattern silently rejects a letter between the hyphen and the serial** — findings tagged with a reviewer-round letter went invisible to both consumers while unrelated tags in evidence prose were counted instead, so the tool reported a confident number drawn entirely from noise; publish conforming/non-conforming examples side by side and consider widening the pattern. Related: the ID pattern over-matches domain tags inside evidence prose (13 of this target's own historical incident tags registered as unparseable findings) — match item IDs at entry-heading position only. **Register over the 120-line hard cap for the third consecutive run, cause now diagnosed**: the grammar doc budgets register-shaped files per entry while the extractor applies a flat file cap, so the cap measures finding count, not prose padding — reconcile or exempt. **Assess-only does NOT structurally cap tier at PROBABLE, contradicting the prior row** — 4 findings reached CONFIRMED by executed proof (3 repro scripts written outside the target, 1 failing read-only gate run); assess-only forbids changing the target, not executing against it, so restate the comparison rule as capping remediation, not reproduction. Findings routed to a themed sibling report are invisible to the extractor (undercounted this run by 13). A ledger row written a turn before its dispatch call is a phantom row indistinguishable from a hung operative — write-at-dispatch-time must mean atomically with the call. Lead model changed 3x mid-run (operator-initiated); identity still absent from artifacts and hand-stamped, reinforcing the prior row's unresolved request. One slice returned a well-formed zero-finding result with 5 leads each traced to a dominating guard — the metrics vocabulary cannot distinguish a covered negative from a failed dispatch |
| 2026-07-30 | code-ops-suite@1.29.0 | large TypeScript monorepo — real-time telephony platform with a payment lane, chat-bot operator surface, and infrastructure-as-code | 20 | 0.19 (9 of 47) | 0.83 (5 of 6 paneled; 3 repro-exempt) | Second run against the control target, so deltas are like-for-like. **Six prior lessons verifiably landed** — the strongest signal in the row: the per-entry register budget replaced the flat file cap (a 47-finding register was no longer penalized for length), ledger phase markers carrying the lead model worked AND the extractor raised its own advisory when the lead changed mid-run (closing a request open across two rows), round-letter item ids parsed where an identical shape previously counted zero, entry-heading-position matching held so ids in evidence prose did not inflate the count, the themed-sibling-report warning fired and caught a real duplication in this run's own artifacts, and covered negatives are now counted (68 this run) so examined-and-clean is finally distinguishable from a failed dispatch. **One recurrence, twice in one run: the phantom ledger row (L-013)** — rows written by a batch artifact edit with no accompanying dispatch call, indistinguishable from a hung operative, and this time also asserted to the operator as work in flight; restating the write-atomically rule in prose has now failed twice and wants a mechanical check. **The CONFIRMED ratio is not comparable across runs as currently defined**: it fell 0.57 to 0.19 mostly because six findings arrived labelled CONFIRMED on purely static reasoning and were re-tiered down by the lead — the metric measures labelling discipline as much as evidence depth, so enforce the tier definition at the operative boundary or add an executed-evidence flag. **Lens diversity, not redundancy, killed the headline finding**: a critical rested on a misread config declaration and two of three independent readers (the original finder plus one panelist) repeated the same misread; only a differently-lensed panelist caught it, and that panelist independently found the sharper real defect the original missed — an odd panel of identical skeptics would have confirmed the wrong answer 2-1. Paneled coverage went 1-of-1 to 6-of-6 eligible, and two further findings were reduced rather than killed (an 'unbounded' timeout was really bounded near 340s by three third-party defaults; a firewall finding lost its misleading-diagnosis claim). **Four blocked lead turns are a real artifact-loss risk**: the two operative reports that landed during them carried high-severity findings existing nowhere but the conversation — writing each report to disk in the turn it lands is what saved the rest. Four smaller instrument gaps share one family, a grammar fix applied in one place and not its mirror: the per-entry budget has no entry terminator (trailing covered-negative lines are attributed to the final entry and reliably trip its hard cap), the refutation log still matches ids mid-line so explanatory prose becomes an unparseable receipt, the sibling-report warning does not recurse into subdirectories so per-slice reports stay invisible, and the metrics tool's own output trips its own sibling warning. Also: the note template prescribes a prose severity-mix shape its own validator rejects as a Unix path, so a note written exactly to template fails closed; and a ledger row cannot express both failed and retried, so failed-rate and redispatch-rate are mutually exclusive for one unit. Verified NOT a defect: the note gate is genuinely fail-closed (exit 1 on a dirty note) — an apparent exit-0 was an artifact of piping through tail. |
| 2026-07-31 | code-ops-suite@1.34.0 | mid-size TypeScript SaaS web application (payments, background jobs, third-party data acquisition, public scoring surface) | 23 | 0.05 (4 of 78) | 0.86 (6 of 7 paneled) | Second run against this target class (prior: 2026-07-28, assess-only), so deltas are like-for-like: dispatches 16 to 23, findings 49 to 78, survival 0.83 to 0.857, paneled coverage 5 of 6 eligible to 7 of 7, operative tokens roughly 1.53M to 1.99M. **The CONFIRMED ratio fell 0.71 to 0.051 and this is not a quality regression.** The prior row earned its ratio partly by operatives labelling static reasoning CONFIRMED; this run inlined the operative-boundary tier rule into every dispatch brief and every CONFIRMED label carried an executed repro in the operative's own transcript, with zero labels re-tiered by the lead. L-020 verifiably landed, and the two ratios are not comparable across that change. **Paneling changed three of seven eligible highs** - the best per-panel yield recorded: one killed outright on reachability (a feature flag defaulting off dominating the write), one had its impact mechanism partly disproved, one had its mechanism corrected in the finder's favour while the refuter independently surfaced an aggravating fact the finder missed (consent copy promising users something the code does not enforce). The strongest verification came from a refuter settling a vendor-API question against the installed package's own types and request encoder rather than memory, and flagging the one memory-sourced fact as such. **The most instructive result was not a panel**: the finding the panel killed had already been found twice independently, and the adversarial reviewer located the containing guard unaided while the slice reviewer missed it - lens overlap reached the panel's verdict before any refutation dispatch ran (L-030). Covered-negative reporting was the highest-value change: 56 covered negatives against 78 findings made examined-and-clean legible and made the genuinely unswept set visible as its own category. **Three recurrences.** L-019: four units failed and all four were redispatched, but the snapshot-only basis reports a 0 percent redispatch rate - compounded by L-013, the ledger being hand-authored rather than generated so no write journal exists at all. L-021 recurred as a near-miss rather than a loss: a blocked lead turn killed the originating session mid-run, leaving four dangling dispatches whose reports were never written; they were marked failed and redispatched under the failure ladder. **Four new instrument gaps.** The config line cannot express a split lead (L-025) - this run had one, after the lead was blocked by its own safeguards, so it cannot serve as an orchestration-experiment arm. Two standing rules directly conflict (L-026): persist every operative report verbatim, versus findings live only in the register - a faithful sweep trips the sibling warning on its own reports folder every time, ten times here. The anchor checker resolves bare filenames anywhere in an entry, so 11 of 36 entries came back ambiguous and buried the 4 genuinely drifted anchors (L-027). A generated repository map trips the flat hard line cap, the same false positive already fixed for the metrics tool's own output (L-028). Also: the panel triage cap of five fired on a run where paneling all seven was correct (L-029). Atlas leg: 8 sections, 1 consumed FRESH, 4 refreshed and re-stamped, 3 falsified - the falsified claims were load-bearing (a billed-attempt cap stated as 3 when code allows 5, an unqualified control-flow rule that no longer holds, an unconditional match rule that now requires corroboration, and an UNVERIFIED note disproved by twelve invalidation sites). Three sections were deliberately left STALE with an inbox note rather than stamped, because the sweep did not cover their scope deeply enough to earn the stamp. |
| 2026-08-09 | code-ops-suite@1.34.0 | mid-size TypeScript SaaS web application (payments, background jobs, third-party data acquisition, public scoring surface) | 22 | 0.39 (49 of 126) | 0.89 (16 of 18 paneled; 9 repro-exempt) | Third run against this target class and the first run in the series to carry a config line, so it is the baseline for orchestration-configuration comparison rather than a member of the pre-registered control-target experiment. Deltas are like-for-like against 2026-07-31 (same class, same suite version, same track): dispatches 23 to 22, findings 78 to 126, CONFIRMED 4 to 49, covered negatives 56 to 176, paneled 7 of 7 eligible to 18 of 18, survival 0.857 to 0.889, operative tokens roughly 1.99M to 2.98M, slices 10 to 12, highs 6 to 24. **CONFIRMED per 100k operative tokens moved 0.20 to 1.65, an eight-fold improvement in cost per proven finding, and the ratio rise 0.05 to 0.39 is not looser labelling** - the operative-boundary tier rule was inlined identically to the prior run and every one of the 49 carries an executed repro in the finder own transcript. What changed is what operatives chose to execute: component-function probes, real document renders, executed greps proving a symbol has zero callers, rebuilt policy objects run against platform APIs, and a minimal protocol server stood up over a real socket to prove a client library resolves rather than rejects. The tier rule was already sufficient; the prior run simply lacked operatives reaching for execution on questions that looked static. **Panels are a tier-promotion mechanism, not only a kill mechanism (L-034)** - of 18 paneled, 2 killed and 16 survived, but four survivals were promoted from argued-statically to executed-confirmed by the refuter doing the proof the finder had not, and six had characterization materially corrected in both directions. **A kill can be conditional on another finding and receipts cannot say so (L-032)**: one finding was refuted by a guard that a concurrent panel proved does not work, with the refuting operative predicting the crack and marking it unverified while the other confirmed it by execution. **The lead is a false-premise generator (L-033)**: six premises the lead wrote into briefs were measured false, four lead-authored, all caught only because briefs from wave two onward carried an explicit invitation to reject a bad premise; the mechanism is that inventory facts from one operative report become causal claims when synthesised into another operative brief. **Three recurrences.** L-027 recurred and worsened - every register entry came back needing re-triage and not one verdict concerned an entry actual location-and-anchor pair, all being bare filenames in evidence prose; hand-checking the real pairs returned all fresh, so the masking rate rises with evidence quality and the check degrades exactly as finding discipline improves. L-028 recurred verbatim on the generated repository map. L-029 fired on a run where paneling everything was correct. **Two prior lessons verifiably landed**: orchestration rates are journal-derived for the first time in the series, because the run drove the ledger exclusively through its generator - the tool fix had shipped rounds earlier and what was missing was skill text telling the lead to use it (L-036); and the standing report-versus-register conflict was resolved with no tool change by splitting each report at persist time, findings into the register at entry position and everything else into a context file, after which the sibling warning never fired. Also new: a refutation receipt whose key is not a valid item id is dropped silently, counted as neither a receipt nor unparseable (L-031), and the phase-zero scoping explorer should be required to return a disjoint slice decomposition and an explicit unsliced list rather than leaving it to the lead to request (L-035) - run that way it produced the entire slice plan, the risk inventory every later brief quoted, and the highest-value unchecked item in the run. Atlas leg: 8 sections, 0 consumed fresh (nothing entered the run as truth), 2 refreshed and re-stamped, 2 falsified; both falsified claims were count-and-identity claims about configured structure rather than behavioural claims, the class an atlas is most likely to get wrong because nothing breaks when they rot. The six stale-only sections were verified as substantively holding and deliberately left stale with an inbox note. |
| 2026-08-10 | code-ops-suite@1.34.0 | large TypeScript monorepo, real-time telephony platform with a payment lane, chat-bot operator surface, and infrastructure-as-code | 6 | 0.28 (5 of 18) | 1.00 (4 of 4 paneled; 3 repro-exempt) | A brief rejected at the concurrency boundary again required explicit failed-dispatch handling and shape-gated retry. Persisted operative reports again conflicted with register-only counting and produced six sibling-report warnings. The generated repository map again exceeded the generic artifact cap. One stale atlas section carried a disproven tooling claim; leaving it stale and recording an inbox observation prevented a false refresh. |
| 2026-08-11 | code-ops-suite@1.34.0 | Mid-size polyglot research operations platform | 7 | 0.57 (8 of 14) | 1.00 (1 of 1 paneled; 8 repro-exempt) | Exact-configuration assess-only calibration. All operative units reported without retry. Operatives again labelled static reasoning as CONFIRMED, so the 0.57 confirmed ratio measures labelling discipline as much as evidence depth. The generated repository map tripped the flat summary line cap again. Initial atlas population refreshed eight of nine sections; one remained stale with an inbox note after an accepted scope became uncheckable under the internal exclusion. The target's unknown-not-zero contract showed the need for a dedicated sweep of aggregate and empty-state projections. No target code changed. |
| 2026-08-18 | code-ops-suite@1.42.0 | The code-ops marketplace itself, plus the three sibling repositories whose documentation vaults it standardized | 15 | 0.72 (21 of 29) | n/a (0 paneled) | First self-directed run recorded in this store: the target is this repository and the three sibling repositories that adopted the vault standard, so no sanitization applies and the one-way channel rule is not engaged. Scope: the vault standard v2 rollout, the conform orchestrator, and the conformance-calibration integration. **The quality numbers are derived, not measured by a metrics run.** Every finding count is read off the deep-review record of the six pull requests this session shipped through review: 58, 60, 61, 62, 63, and 64. The derivation rule is mechanical. One finding is counted for each distinct inline review comment, plus one for each distinct finding stated in a review body but not filed inline. Comments that request no action are excluded, which removed exactly one comment, the case 12c confirmation on PR 62. A finding counts toward `confirmed` only when its own text carries the literal label CONFIRMED. Per pull request, findings and CONFIRMED: 58 gives 10 and 7, 60 gives 5 and 5, 61 gives 4 and 2, 62 gives 3 and 2, 63 gives 5 and 4, 64 gives 2 and 1. Severity is the reviewers own Blocking, Should-fix, and Nit grouping, mapped to high, medium, and nit. **No refutation panel ran.** The store records that as zero panels with `panelEligible` null, the shape R-001 through R-003 already use for a phase that was not measured. It is not a survival rate of zero. Dispatch accounting comes from the run ledger in the vault: 15 dispatches, D-001 through D-015, none dangling, none failed, none redispatched. One continuation reused a live operative in its own context rather than opening a new dispatch, which is a reuse and not a failure. The `config` field is deliberately absent. The ledger records operative roles and tiers, not model classes, and the schema treats an absent config as unrecorded rather than defaulted. Three mechanical enforcements landed and are counted: the fail-closed `check-vault-standard.mjs` with its `evals/vault-standard/` fixture pair and CI step, lint check 21 over the handbook technique index, and lint check 22 over the skill-composition map. A fourth, the conformance grammar in `calibration-metrics.mjs`, is still on an unmerged branch and is not counted. The shape of the review record is itself the finding. The CONFIRMED ratio of 0.72 is high because the reviews ran against this repository, where a reviewer can reproduce anything at will. Two blocking defects both came from the first pull request of the arc, and four later pull requests were successive rounds closing the previous round findings on one lint check, which is the pattern L-042 names. |
