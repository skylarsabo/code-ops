Dispatch notes for this run (freeform prose, not the pipe-table row grammar):

explorer mapped the auth module and reported back with AUTH_MAP.md.
mech renamed the config field, diff attached, reported.
