# Findings (fixture)

## FIND-001 — first entry, tight
Tier: CONFIRMED
Severity: high
Location: a.js:1

## Method notes

The auth slice was swept with the tracer lens, pass 1.
The token slice was swept with the tracer lens, pass 2.
The cache slice was swept with the tracer lens, pass 3.
The queue slice was swept with the tracer lens, pass 4.
The retry slice was swept with the tracer lens, pass 5.
The auth slice was swept with the tracer lens, pass 6.
The token slice was swept with the tracer lens, pass 7.
The cache slice was swept with the tracer lens, pass 8.
The queue slice was swept with the tracer lens, pass 9.
The retry slice was swept with the tracer lens, pass 10.
The auth slice was swept with the tracer lens, pass 11.
The token slice was swept with the tracer lens, pass 12.
The cache slice was swept with the tracer lens, pass 13.
The queue slice was swept with the tracer lens, pass 14.
The retry slice was swept with the tracer lens, pass 15.
The auth slice was swept with the tracer lens, pass 16.
The token slice was swept with the tracer lens, pass 17.
The cache slice was swept with the tracer lens, pass 18.
The queue slice was swept with the tracer lens, pass 19.
The retry slice was swept with the tracer lens, pass 20.
The auth slice was swept with the tracer lens, pass 21.
The token slice was swept with the tracer lens, pass 22.
The cache slice was swept with the tracer lens, pass 23.
The queue slice was swept with the tracer lens, pass 24.
The retry slice was swept with the tracer lens, pass 25.

## FIND-002 — last entry, tight
Tier: PROBABLE
Severity: low
Location: b.js:2

NO-FINDINGS: slice-01 — traced every call site, all branches re-check.
NO-FINDINGS: slice-02 — traced every call site, all branches re-check.
NO-FINDINGS: slice-03 — traced every call site, all branches re-check.
NO-FINDINGS: slice-04 — traced every call site, all branches re-check.
NO-FINDINGS: slice-05 — traced every call site, all branches re-check.
NO-FINDINGS: slice-06 — traced every call site, all branches re-check.
NO-FINDINGS: slice-07 — traced every call site, all branches re-check.
NO-FINDINGS: slice-08 — traced every call site, all branches re-check.
NO-FINDINGS: slice-09 — traced every call site, all branches re-check.
NO-FINDINGS: slice-10 — traced every call site, all branches re-check.
NO-FINDINGS: slice-11 — traced every call site, all branches re-check.
NO-FINDINGS: slice-12 — traced every call site, all branches re-check.
NO-FINDINGS: slice-13 — traced every call site, all branches re-check.
NO-FINDINGS: slice-14 — traced every call site, all branches re-check.
NO-FINDINGS: slice-15 — traced every call site, all branches re-check.
NO-FINDINGS: slice-16 — traced every call site, all branches re-check.
NO-FINDINGS: slice-17 — traced every call site, all branches re-check.
NO-FINDINGS: slice-18 — traced every call site, all branches re-check.
NO-FINDINGS: slice-19 — traced every call site, all branches re-check.
NO-FINDINGS: slice-20 — traced every call site, all branches re-check.
NO-FINDINGS: slice-21 — traced every call site, all branches re-check.
NO-FINDINGS: slice-22 — traced every call site, all branches re-check.
NO-FINDINGS: slice-23 — traced every call site, all branches re-check.
NO-FINDINGS: slice-24 — traced every call site, all branches re-check.
NO-FINDINGS: slice-25 — traced every call site, all branches re-check.
