# Calibration note (fixture, no Machine block)

Recall held steady across the run and the prose is structurally clean: no paths, no
fences, no URLs, no emails. What it is missing is the Machine block the sanitized-note
template now requires, so the note cannot be ingested into a run doc and the gate
must fail closed on it.
