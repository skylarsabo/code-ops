[
  {
    "id": "L-001",
    "class": "instrument",
    "title": "register flat cap wrong at real scale",
    "statement": "The findings-register length budget applied a flat per-file line cap, so a legitimately large real-scale backlog tripped the hard bound every run. The cap measures finding count rather than prose padding; register-shaped artifacts need a per-entry budget instead.",
    "firstSeen": "R-001"
  },
  {
    "id": "L-002",
    "class": "instrument",
    "title": "extractor cannot parse prose-shaped artifacts; publish grammars, warn on zero-parse",
    "statement": "The metrics extractor parsed zero items from present, non-empty prose-shaped artifacts and reported a silent zero that read as \"nothing here\". Publish the artifact grammars with conforming examples and warn when a non-empty artifact yields zero parsed items.",
    "firstSeen": "R-001"
  },
  {
    "id": "L-003",
    "class": "suite",
    "title": "accepted-by-design checks must read dated decision records",
    "statement": "Reviewers repeatedly re-raised deliberate design choices as findings because the accepted-by-design check had no dated decision record to consult. An accepted-by-design verdict must cite a dated ADR or equivalent rather than reviewer memory.",
    "firstSeen": "R-001"
  },
  {
    "id": "L-004",
    "class": "suite",
    "title": "refutation panels mandatory at high severity (they killed overstated/harmful fixes)",
    "statement": "Refutation panels killed two overstated high-severity findings, one of whose proposed fixes would have been actively harmful. Paneling stays mandatory at high severity; a paneling-coverage metric is the candidate mechanical backstop.",
    "firstSeen": "R-001"
  },
  {
    "id": "L-005",
    "class": "instrument",
    "title": "executed model identity absent from artifacts; tier substitution invisible",
    "statement": "No run artifact recorded which model actually executed a dispatch or led a phase, so a silent tier substitution mid-run left no trace and the tier-mix metric was unreconstructable. Resolved model identity must be stamped into the dispatch ledger per row and per phase.",
    "firstSeen": "R-002"
  },
  {
    "id": "L-006",
    "class": "protocol",
    "title": "assess-only structurally caps CONFIRMED (DISPROVEN — superseded by L-012)",
    "statement": "Recorded as a protocol rule that assess-only runs cannot reach CONFIRMED because operatives cannot execute, so ratios were only comparable within-track. A later run disproved it by reaching CONFIRMED four times through executed proof; superseded by L-012.",
    "firstSeen": "R-002"
  },
  {
    "id": "L-007",
    "class": "instrument",
    "title": "artifact grammars must be published with conforming examples + zero-parse warning",
    "statement": "Extraction only started working after item IDs, tier/severity fields and ledger columns were reshaped by hand, because the expected grammars were never published. Ship the grammars with conforming examples and pair them with a zero-items-in-non-empty-file warning.",
    "firstSeen": "R-002"
  },
  {
    "id": "L-008",
    "class": "instrument",
    "title": "redaction gate crashes uncaught on a run folder",
    "statement": "The redaction gate threw an uncaught error with a non-fail-closed exit code when handed a run folder, which is the invocation its own docs imply. A gate that crashes instead of failing closed silently stops gating.",
    "firstSeen": "R-002"
  },
  {
    "id": "L-009",
    "class": "instrument",
    "title": "item-ID pattern rejects round letter between hyphen and serial",
    "statement": "Findings tagged with a reviewer-round letter between the hyphen and the serial were invisible to both consumers, while unrelated tags in evidence prose were counted instead, so the tool reported a confident number drawn entirely from noise. Widen the pattern and publish conforming and non-conforming examples side by side.",
    "firstSeen": "R-003"
  },
  {
    "id": "L-010",
    "class": "instrument",
    "title": "item-ID pattern over-matches domain tags in evidence prose",
    "statement": "An unanchored item-ID scan counted domain tags appearing inside evidence prose as findings. Item IDs must be matched at entry-heading position only.",
    "firstSeen": "R-003"
  },
  {
    "id": "L-011",
    "class": "instrument",
    "title": "findings in themed sibling reports invisible to extractor",
    "statement": "Findings routed into a themed sibling report never reached any metric, undercounting one run substantially. A sibling artifact carrying register-shaped entries must warn that its findings are not counted.",
    "firstSeen": "R-003"
  },
  {
    "id": "L-012",
    "class": "protocol",
    "title": "assess-only caps remediation, not reproduction",
    "statement": "Assess-only forbids changing the target, not executing against it: findings reached CONFIRMED via repro scripts written outside the target and a failing read-only gate run. The comparison rule is restated as capping remediation rather than reproduction.",
    "firstSeen": "R-003"
  },
  {
    "id": "L-013",
    "class": "protocol",
    "title": "ledger row must be written atomically with the dispatch call (phantom-row hazard)",
    "statement": "A ledger row written a turn before its dispatch call is a phantom row indistinguishable from a hung operative. Write-at-dispatch-time must mean atomically with the call itself.",
    "firstSeen": "R-003"
  },
  {
    "id": "L-014",
    "class": "instrument",
    "title": "metrics vocabulary cannot distinguish covered negative from failed dispatch",
    "statement": "A slice returned a well-formed zero-finding result with every lead traced to a dominating guard, and the metrics reported it identically to a dispatch that failed. The vocabulary needs an explicit covered-negative declaration.",
    "firstSeen": "R-003"
  },
  {
    "id": "L-015",
    "class": "instrument",
    "title": "The register per-entry length budget has no entry terminator, so trailing covered-negative lines are attributed to the final entry and reliably trip its hard cap",
    "statement": "The register per-entry length budget has no entry terminator, so trailing covered-negative lines are attributed to the final entry and reliably trip its hard cap.",
    "firstSeen": "R-004"
  },
  {
    "id": "L-016",
    "class": "instrument",
    "title": "The refutation log still matches item identifiers mid-line, so explanatory prose citing a finding becomes an unparseable receipt",
    "statement": "The refutation log still matches item identifiers mid-line, so explanatory prose citing a finding becomes an unparseable receipt; the entry-heading-position fix applied to registers was never mirrored to refutation logs.",
    "firstSeen": "R-004"
  },
  {
    "id": "L-017",
    "class": "instrument",
    "title": "The themed-sibling-report warning does not recurse into subdirectories, so per-slice reports carrying entry-shaped findings remain invisible to every register consumer",
    "statement": "The themed-sibling-report warning does not recurse into subdirectories, so per-slice reports carrying entry-shaped findings remain invisible to every register consumer.",
    "firstSeen": "R-004"
  },
  {
    "id": "L-018",
    "class": "instrument",
    "title": "The metrics tool's own output file trips its own sibling-report warning, because the per-entry length lines it emits parse as register entries at line start",
    "statement": "The metrics tool's own output file trips its own sibling-report warning, because the per-entry length lines it emits parse as register entries at line start.",
    "firstSeen": "R-004"
  },
  {
    "id": "L-019",
    "class": "instrument",
    "title": "A ledger row cannot express both that a unit failed and that it was retried, so failed rate and redispatch rate are mutually exclusive for one unit and the pair understates recovery",
    "statement": "A ledger row cannot express both that a unit failed and that it was retried, so failed rate and redispatch rate are mutually exclusive for one unit and the pair understates recovery.",
    "firstSeen": "R-004"
  },
  {
    "id": "L-020",
    "class": "instrument",
    "title": "Operatives label static reasoning as CONFIRMED, so the confirmed ratio measures labelling discipline as much as evidence depth",
    "statement": "Operatives label static reasoning as CONFIRMED, so the confirmed ratio measures labelling discipline as much as evidence depth; enforce the tier definition at the operative boundary or add an explicit executed-evidence flag.",
    "firstSeen": "R-004"
  },
  {
    "id": "L-021",
    "class": "protocol",
    "title": "Persist every operative report to disk in the turn it lands, before any other work",
    "statement": "Persist every operative report to disk in the turn it lands, before any other work; four blocked lead turns nearly discarded two high-severity reports that existed only in conversation.",
    "firstSeen": "R-004"
  },
  {
    "id": "L-022",
    "class": "protocol",
    "title": "A brief that never reaches its operative is indistinguishable at the ledger from a completed dispatch until the report is read, so gate every report on shape before counting the unit covered",
    "statement": "A brief that never reaches its operative is indistinguishable at the ledger from a completed dispatch until the report is read, so gate every report on shape before counting the unit covered.",
    "firstSeen": "R-004"
  },
  {
    "id": "L-023",
    "class": "instrument",
    "title": "The note template prescribes a prose severity-mix shape its own validator rejects as a Unix path, so a note written exactly to template fails closed",
    "statement": "The note template prescribes a prose severity-mix shape its own validator rejects as a Unix path, so a note written exactly to template fails closed; reconcile the documented shape with the enforced one.",
    "firstSeen": "R-004"
  },
  {
    "id": "L-024",
    "class": "suite",
    "title": "Lens diversity rather than redundancy is what kills a wrong finding: two of three independent readers repeated the same misread of a configuration declaration, and only a differently-lensed panelist caught it",
    "statement": "Lens diversity rather than redundancy is what kills a wrong finding: two of three independent readers repeated the same misread of a configuration declaration, and only a differently-lensed panelist caught it.",
    "firstSeen": "R-004"
  },
  {
    "id": "L-025",
    "class": "instrument",
    "title": "The configuration line records a single lead model class and cannot express a lead that changed mid-run, which is precisely the event the pre-registered orchestration experiment exists to detect",
    "statement": "The configuration line records a single lead model class and cannot express a lead that changed mid-run, which is precisely the event the pre-registered orchestration experiment exists to detect; this run had a split lead after the first lead was blocked by its own safeguards, so the field was omitted rather than guessed and the run cannot serve as an experiment arm.",
    "firstSeen": "R-005"
  },
  {
    "id": "L-026",
    "class": "protocol",
    "title": "Two standing rules cannot both be satisfied: one requires persisting every operative report verbatim in the turn it lands, the other requires findings to live only in the register",
    "statement": "Two standing rules cannot both be satisfied: one requires persisting every operative report verbatim in the turn it lands, the other requires findings to live only in the register. A faithfully executed sweep therefore always trips the not-counted sibling warning on its own reports folder, ten times here. Exempt the reports folder or drop the verbatim-persist rule.",
    "firstSeen": "R-005"
  },
  {
    "id": "L-027",
    "class": "instrument",
    "title": "The register anchor checker resolves bare filenames appearing anywhere in an entry, so evidence prose naming common basenames produced ambiguous verdicts for eleven of thirty-six entries and buried the four genuinely drifted anchors",
    "statement": "The register anchor checker resolves bare filenames appearing anywhere in an entry, so evidence prose naming common basenames produced ambiguous verdicts for eleven of thirty-six entries and buried the four genuinely drifted anchors; framework-style bracketed dynamic route segments additionally report as escaping the repository root.",
    "firstSeen": "R-005"
  },
  {
    "id": "L-028",
    "class": "instrument",
    "title": "A generated repository map trips the flat hard line cap applied to run summaries, the same false positive already recorded for the metrics tool's own output",
    "statement": "A generated repository map trips the flat hard line cap applied to run summaries, the same false positive already recorded for the metrics tool's own output; generated artifacts need the same exemption.",
    "firstSeen": "R-005"
  },
  {
    "id": "L-029",
    "class": "suite",
    "title": "The panel triage cap of five was exceeded by seven eligible high-severity findings, and paneling all seven changed three of them materially, so the cap fired on a run where paneling everything was the correct call",
    "statement": "The panel triage cap of five was exceeded by seven eligible high-severity findings, and paneling all seven changed three of them materially, so the cap fired on a run where paneling everything was the correct call; make the cap a scoping checkpoint rather than a signal to sample.",
    "firstSeen": "R-005"
  },
  {
    "id": "L-030",
    "class": "suite",
    "title": "Two independent operatives with different lenses over the same surface converged on the same defect and one of them located the containing guard unaided, reaching the panel's eventual verdict without a refutation dispatch",
    "statement": "Two independent operatives with different lenses over the same surface converged on the same defect and one of them located the containing guard unaided, reaching the panel's eventual verdict without a refutation dispatch; deliberate lens overlap may substitute for paneling where briefs can both reach a surface.",
    "firstSeen": "R-005"
  },
  {
    "id": "L-031",
    "class": "instrument",
    "title": "a refutation receipt whose key is not a valid item id is dropped silently, counted as neither a receipt nor an unparseable one, so pre-finding kills are invisible to the survival rate",
    "statement": "a refutation receipt whose key is not a valid item id is dropped silently, counted as neither a receipt nor an unparseable one, so pre-finding kills are invisible to the survival rate",
    "firstSeen": "R-006"
  },
  {
    "id": "L-032",
    "class": "protocol",
    "title": "a refutation can be conditional on another finding being fixed, and the receipt grammar cannot express a conditional verdict, so the store reads a conditional kill as a clean one",
    "statement": "a refutation can be conditional on another finding being fixed, and the receipt grammar cannot express a conditional verdict, so the store reads a conditional kill as a clean one",
    "firstSeen": "R-006"
  },
  {
    "id": "L-033",
    "class": "protocol",
    "title": "the lead converts inventory facts into causal claims when synthesising one operative report into another operative brief",
    "statement": "the lead converts inventory facts into causal claims when synthesising one operative report into another operative brief; put the challenge-the-premise instruction in the standing brief skeleton and carry provenance inline",
    "firstSeen": "R-006"
  },
  {
    "id": "L-034",
    "class": "suite",
    "title": "refutation panels promote tiers as well as killing findings, and a panel practice measured only by kill rate under-uses them",
    "statement": "refutation panels promote tiers as well as killing findings, and a panel practice measured only by kill rate under-uses them",
    "firstSeen": "R-006"
  },
  {
    "id": "L-035",
    "class": "instrument",
    "title": "the phase-zero scoping explorer should be required to return a disjoint slice decomposition and an explicit unsliced list rather than leaving it to the lead to request",
    "statement": "the phase-zero scoping explorer should be required to return a disjoint slice decomposition and an explicit unsliced list rather than leaving it to the lead to request",
    "firstSeen": "R-006"
  },
  {
    "id": "L-036",
    "class": "protocol",
    "title": "the sweep skill does not tell the lead to drive the dispatch ledger through its generator, which is the only thing that produces the write journal the orchestration rates need",
    "statement": "the sweep skill does not tell the lead to drive the dispatch ledger through its generator, which is the only thing that produces the write journal the orchestration rates need",
    "firstSeen": "R-006"
  },
  {
    "id": "L-037",
    "class": "instrument",
    "title": "A stale atlas section carried a disproven tooling claim, so stale sections must remain leads until claim-level review earns refresh",
    "statement": "A stale atlas section carried a disproven tooling claim, so stale sections must remain leads until claim-level review earns refresh",
    "firstSeen": "R-007"
  },
  {
    "id": "L-038",
    "class": "instrument",
    "title": "an accepted recursive atlas scope can become dead when the checker adds its internal exclusion, and no sanctioned scope repair is available",
    "statement": "an accepted recursive atlas scope can become dead when the checker adds its internal exclusion, and no sanctioned scope repair is available",
    "firstSeen": "R-008"
  },
  {
    "id": "L-039",
    "class": "suite",
    "title": "repositories with an explicit unknown-not-zero contract need a dedicated sweep of aggregate and empty-state projections",
    "statement": "repositories with an explicit unknown-not-zero contract need a dedicated sweep of aggregate and empty-state projections",
    "firstSeen": "R-008"
  },
  {
    "id": "L-040",
    "class": "suite",
    "title": "a fail-closed conformance checker shipped with no CI step and no fixture pair, so the standard it guards could drift silently in the repository that authored it",
    "statement": "a fail-closed conformance checker shipped with no CI step and no fixture pair, so the standard it guards could drift silently in the repository that authored it",
    "firstSeen": "R-009"
  },
  {
    "id": "L-041",
    "class": "instrument",
    "title": "a new lint check landed with no regression eval case, and the advisory that flags an unreferenced script does not gate, so nothing stopped a later refactor from dropping the check",
    "statement": "a new lint check landed with no regression eval case, and the advisory that flags an unreferenced script does not gate, so nothing stopped a later refactor from dropping the check",
    "firstSeen": "R-009"
  },
  {
    "id": "L-042",
    "class": "protocol",
    "title": "one commit that widens a scan in one direction and narrows it in another opens a silent bypass, so each direction needs its own eval case in the same commit",
    "statement": "one commit that widens a scan in one direction and narrows it in another opens a silent bypass, so each direction needs its own eval case in the same commit",
    "firstSeen": "R-009"
  },
  {
    "id": "L-043",
    "class": "suite",
    "title": "The version 4 run contract compiler refuses any lead below the frontier tier and the security campaign checker requires that contract, so a strong-tier lead can compile neither and the protocol's own pre-registered strong-lead experiment arms cannot run under the current suite",
    "statement": "The version 4 run contract compiler refuses any lead below the frontier tier and the security campaign checker requires that contract, so a strong-tier lead can compile neither and the protocol's own pre-registered strong-lead experiment arms cannot run under the current suite",
    "firstSeen": "R-010"
  },
  {
    "id": "L-044",
    "class": "instrument",
    "title": "The host capabilities writer requires a git-ignored output while the placement check expects run artifacts in the vault's tracked runs folder, so version 4 runtime artifacts cannot sit beside the run they describe in a vault-tracked repository",
    "statement": "The host capabilities writer requires a git-ignored output while the placement check expects run artifacts in the vault's tracked runs folder, so version 4 runtime artifacts cannot sit beside the run they describe in a vault-tracked repository",
    "firstSeen": "R-010"
  },
  {
    "id": "L-045",
    "class": "instrument",
    "title": "The anchor revalidator's reference grammar admits no square brackets or spaces in path segments, so framework dynamic-route paths read as escaping the root and spaced paths read as gone, flagging most of a register whose anchors a lead exact check found all fresh",
    "statement": "The anchor revalidator's reference grammar admits no square brackets or spaces in path segments, so framework dynamic-route paths read as escaping the root and spaced paths read as gone, flagging most of a register whose anchors a lead exact check found all fresh",
    "firstSeen": "R-010"
  },
  {
    "id": "L-046",
    "class": "instrument",
    "title": "An atlas section can assert facts about paths outside its own declared scope, so its freshness digest cannot detect their falsification",
    "statement": "An atlas section can assert facts about paths outside its own declared scope, so its freshness digest cannot detect their falsification; a section read fresh while its route-module counts were wrong because the routes and their guard test lay outside the scope",
    "firstSeen": "R-010"
  },
  {
    "id": "L-047",
    "class": "protocol",
    "title": "The executed-repro panel exemption is only as strong as the repro's mock boundary",
    "statement": "The executed-repro panel exemption is only as strong as the repro's mock boundary; of two confirmed highs whose repros mocked the database and dispatch layers, an intent-lens panel cut one by two severity levels, so a mock-bounded repro should not exempt a high from refutation",
    "firstSeen": "R-010"
  },
  {
    "id": "L-048",
    "class": "protocol",
    "title": "Operative-started background test runners can outlive the reported dispatch, and neither the ledger nor the report shape gate reveals it",
    "statement": "Operative-started background test runners can outlive the reported dispatch, and neither the ledger nor the report shape gate reveals it; two finished reviewers left runners alive for over fifteen minutes, found only because one mentioned it",
    "firstSeen": "R-010"
  },
  {
    "id": "L-049",
    "class": "protocol",
    "title": "On a target with uncommitted work the run must isolate committed state, and the only snapshot that keeps the atlas leg working is a detached worktree on a short path, because the atlas checker needs git and deep paths exceed the platform path limit",
    "statement": "On a target with uncommitted work the run must isolate committed state, and the only snapshot that keeps the atlas leg working is a detached worktree on a short path, because the atlas checker needs git and deep paths exceed the platform path limit; the skill gives no isolation guidance",
    "firstSeen": "R-010"
  },
  {
    "id": "L-050",
    "class": "instrument",
    "title": "The host's background operative output was not machine-extractable, so persist-on-landing forced the lead to re-emit every operative report, roughly doubling report-sized tokens at the lead tier",
    "statement": "The host's background operative output was not machine-extractable, so persist-on-landing forced the lead to re-emit every operative report, roughly doubling report-sized tokens at the lead tier; operatives should write their own report files and return a pointer",
    "firstSeen": "R-010"
  },
  {
    "id": "L-051",
    "class": "instrument",
    "title": "An atlas exclusivity claim was false when stamped, and freshness certifies only an unchanged scope digest, never the claims, so a claim that was never true survives every freshness check",
    "statement": "An atlas exclusivity claim was false when stamped, and freshness certifies only an unchanged scope digest, never the claims, so a claim that was never true survives every freshness check.",
    "firstSeen": "R-011"
  },
  {
    "id": "L-052",
    "class": "instrument",
    "title": "An atlas module-wide generalization was stamped over a module already containing a counterexample and cited a line beyond its file's length, because atlas authoring has no claim-level check against sibling code or cited line ranges",
    "statement": "An atlas module-wide generalization was stamped over a module already containing a counterexample and cited a line beyond its file's length, because atlas authoring has no claim-level check against sibling code or cited line ranges.",
    "firstSeen": "R-011"
  },
  {
    "id": "L-053",
    "class": "instrument",
    "title": "The dispatch ledger add command mints the next serial id and ignores the contract unit id, so dispatching units out of contract order silently mis-keys rows against a version 4 contract and forces re-key revisions",
    "statement": "The dispatch ledger add command mints the next serial id and ignores the contract unit id, so dispatching units out of contract order silently mis-keys rows against a version 4 contract and forces re-key revisions.",
    "firstSeen": "R-011"
  },
  {
    "id": "L-054",
    "class": "instrument",
    "title": "The dispatch ledger add command accepts a missing actor id that version 4 strict reconciliation later rejects, so the omission surfaces only at finalization, after the append-only journal can no longer be corrected",
    "statement": "The dispatch ledger add command accepts a missing actor id that version 4 strict reconciliation later rejects, so the omission surfaces only at finalization, after the append-only journal can no longer be corrected.",
    "firstSeen": "R-011"
  },
  {
    "id": "L-055",
    "class": "instrument",
    "title": "Runtime replan and checkpoint run strict reconciliation, which fails on every planned unit not yet reported, so a multi-wave version 4 run cannot bind any mid-run contract revision, and once revisions accumulate the receipt chain can never catch up because replan accepts only the next revision",
    "statement": "Runtime replan and checkpoint run strict reconciliation, which fails on every planned unit not yet reported, so a multi-wave version 4 run cannot bind any mid-run contract revision, and once revisions accumulate the receipt chain can never catch up because replan accepts only the next revision.",
    "firstSeen": "R-011"
  },
  {
    "id": "L-056",
    "class": "instrument",
    "title": "The atlas checker reports zero unmapped paths by testing only top-level folders, so tracked source, test, and script files matching no section scope, including whole feature modules, stay invisible to the freshness gate",
    "statement": "The atlas checker reports zero unmapped paths by testing only top-level folders, so tracked source, test, and script files matching no section scope, including whole feature modules, stay invisible to the freshness gate.",
    "firstSeen": "R-011"
  },
  {
    "id": "L-057",
    "class": "instrument",
    "title": "The runtime's context-snapshot verification reports drift on an unchanged tracked tree, so checkpoint, replan and resume all refuse and the runtime chain stays init-only for the whole run",
    "statement": "The runtime's context-snapshot verification reports drift on an unchanged tracked tree, so checkpoint, replan and resume all refuse and the runtime chain stays init-only for the whole run.",
    "firstSeen": "R-012"
  },
  {
    "id": "L-058",
    "class": "instrument",
    "title": "The strict revalidator matches critical, high, low and medium anywhere on the severity line, so the composite line's confidence and risk-if-fixed fields make every finding read as load-bearing or deflated until severity sits on its own line",
    "statement": "The strict revalidator matches critical, high, low and medium anywhere on the severity line, so the composite line's confidence and risk-if-fixed fields make every finding read as load-bearing or deflated until severity sits on its own line.",
    "firstSeen": "R-012"
  },
  {
    "id": "L-059",
    "class": "instrument",
    "title": "The anchor grammar has no escape for a backtick inside a backtick-delimited anchor, so a reviewer quoting a source line that contains one produces an anchor the checker reads as a literal backslash",
    "statement": "The anchor grammar has no escape for a backtick inside a backtick-delimited anchor, so a reviewer quoting a source line that contains one produces an anchor the checker reads as a literal backslash.",
    "firstSeen": "R-012"
  },
  {
    "id": "L-060",
    "class": "instrument",
    "title": "The unit bundle compiler refuses discovery slices above a size ceiling as needing broad context, so the ceiling rather than the slice design decides which operatives get a bounded view, and bundled units did not cost less than unbundled ones at this slice size",
    "statement": "The unit bundle compiler refuses discovery slices above a size ceiling as needing broad context, so the ceiling rather than the slice design decides which operatives get a bounded view, and bundled units did not cost less than unbundled ones at this slice size.",
    "firstSeen": "R-012"
  },
  {
    "id": "L-061",
    "class": "instrument",
    "title": "The atlas writer emits a manifest the target's formatter rejects, so an assess-only run leaves a lint error in an untracked file it created",
    "statement": "The atlas writer emits a manifest the target's formatter rejects, so an assess-only run leaves a lint error in an untracked file it created.",
    "firstSeen": "R-012"
  },
  {
    "id": "L-062",
    "class": "instrument",
    "title": "The handoff checker validates shape but not anchors, so an off-by-one line pointer passes and is caught only by the resuming lead's manual pass",
    "statement": "The handoff checker validates shape but not anchors, so an off-by-one line pointer passes and is caught only by the resuming lead's manual pass.",
    "firstSeen": "R-012"
  },
  {
    "id": "L-063",
    "class": "protocol",
    "title": "A four-lens refutation panel tied two-two on two of four findings and the lead resolved both on the merits, so the contract generator should pin the panel seat count to an odd number as the doctrine already requires",
    "statement": "A four-lens refutation panel tied two-two on two of four findings and the lead resolved both on the merits, so the contract generator should pin the panel seat count to an odd number as the doctrine already requires.",
    "firstSeen": "R-012"
  },
  {
    "id": "L-064",
    "class": "protocol",
    "title": "A lead-filed finding collided with a discovery unit's id because the register merge mints nothing, so lead-authored findings need a reserved id range or a mint step before merge",
    "statement": "A lead-filed finding collided with a discovery unit's id because the register merge mints nothing, so lead-authored findings need a reserved id range or a mint step before merge.",
    "firstSeen": "R-012"
  },
  {
    "id": "L-065",
    "class": "protocol",
    "title": "A fresh atlas section stated that two discovery regexes disagreed when they were identical and the real seam was elsewhere, so an atlas claim that compares two code sites must cite both by line when it is written",
    "statement": "A fresh atlas section stated that two discovery regexes disagreed when they were identical and the real seam was elsewhere, so an atlas claim that compares two code sites must cite both by line when it is written.",
    "firstSeen": "R-012"
  }
]
