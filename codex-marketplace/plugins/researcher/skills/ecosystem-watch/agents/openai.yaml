interface:
  display_name: "Ecosystem Watch"
  short_description: "Use to learn what changed in our stack that needs action: dependency updates, CVEs, deprecations, and new capabilities. Schedulable, discovery only, no code."
  default_prompt: "Use researcher:ecosystem-watch for this task."
policy:
  allow_implicit_invocation: true
