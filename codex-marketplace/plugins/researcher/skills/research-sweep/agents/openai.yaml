interface:
  display_name: "Research Sweep"
  short_description: "Use when you want code-grounded research run end-to-end as one developer-in-the-loop pipeline, local-first with opt-in disclosed web. Intra-plugin orchestrator that researches and proposes only, and writes no code."
  default_prompt: "Use researcher:research-sweep for this task."
policy:
  allow_implicit_invocation: true
