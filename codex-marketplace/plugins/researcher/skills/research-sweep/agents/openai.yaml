interface:
  display_name: "Research Sweep"
  short_description: "Use for code-grounded research as one developer-in-the-loop pipeline, local-first with opt-in disclosed web. Orchestrates and proposes; writes no code."
  default_prompt: "Use researcher:research-sweep for this task."
policy:
  allow_implicit_invocation: true
