interface:
  display_name: "Research Spike"
  short_description: "Use when a task, feature, or plan needs a code-grounded design brief before anyone builds it. Writes no code."
  default_prompt: "Use researcher:research-spike for this task."
policy:
  allow_implicit_invocation: false
