interface:
  display_name: "Library Eval"
  short_description: "Use when you must decide whether to adopt a library or approach, comparing A against B against building it, and want a code-grounded fit assessment, migration cost, and a tiered recommendation. Writes no code."
  default_prompt: "Use researcher:library-eval for this task."
policy:
  allow_implicit_invocation: true
