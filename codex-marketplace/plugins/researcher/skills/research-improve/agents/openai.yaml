interface:
  display_name: "Research Improve"
  short_description: "Use for improvements to our existing code grounded in external best practice, not a generic checklist. Writes no code, and proposes and hands off instead."
  default_prompt: "Use researcher:research-improve for this task."
policy:
  allow_implicit_invocation: true
