interface:
  display_name: "Research Verify"
  short_description: "Use to fact-check a claim, recommendation, or draft research adversarially against sources and our code before anyone acts on it. Review only; writes no code."
  default_prompt: "Use researcher:research-verify for this task."
policy:
  allow_implicit_invocation: true
