interface:
  display_name: "Metadata Leak Audit"
  short_description: "Use to find PII or identifiers leaking in logs, telemetry, errors, headers, or file metadata. Timing and size side channels go to traffic-analysis-resistance."
  default_prompt: "Use privacy-opsec-suite:metadata-leak-audit for this task."
policy:
  allow_implicit_invocation: true
