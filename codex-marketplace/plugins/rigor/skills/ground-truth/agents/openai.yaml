interface:
  display_name: "Ground Truth"
  short_description: "Use first, for the factual baseline before any analysis. Runs the real toolchain and captures ground truth plus a coverage and blind-spot map."
  default_prompt: "Use rigor:ground-truth for this task."
policy:
  allow_implicit_invocation: true
