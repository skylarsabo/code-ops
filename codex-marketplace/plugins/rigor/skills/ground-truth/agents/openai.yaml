interface:
  display_name: "Ground Truth"
  short_description: "Use when you want the factual baseline before any analysis. Runs the real toolchain and captures ground truth plus a coverage and blind-spot map. Run this first."
  default_prompt: "Use rigor:ground-truth for this task."
policy:
  allow_implicit_invocation: true
