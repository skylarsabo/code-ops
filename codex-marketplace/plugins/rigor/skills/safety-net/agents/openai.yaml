interface:
  display_name: "Safety Net"
  short_description: "Use before refactoring or fixing low-coverage code — writes characterization tests that lock current observable behavior."
  default_prompt: "Use rigor:safety-net for this task."
policy:
  allow_implicit_invocation: false
