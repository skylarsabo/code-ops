interface:
  display_name: "Deep Review"
  short_description: "Use when you want a PR/diff reviewed at the verification bar — blocking only on CONFIRMED defects/regressions. The high-rigor counterpart to code-ops-suite:pr-review."
  default_prompt: "Use rigor:deep-review for this task."
policy:
  allow_implicit_invocation: false
