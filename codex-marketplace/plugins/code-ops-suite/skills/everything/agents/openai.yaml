interface:
  display_name: "Everything"
  short_description: "Use for a checkpointed pass over installed plugins, from one plugin pipeline to the cross-plugin superset. Pick plugins and a track: assess-only, full, feature."
  default_prompt: "Use code-ops-suite:everything for this task."
policy:
  allow_implicit_invocation: true
