interface:
  display_name: "Local Review Gate"
  short_description: "Use when deep review, OpSec review, or judgment evals should run locally before a PR, with exact-SHA receipts and optional GitHub status publication."
  default_prompt: "Use code-ops-suite:local-review-gate for this task."
policy:
  allow_implicit_invocation: true
