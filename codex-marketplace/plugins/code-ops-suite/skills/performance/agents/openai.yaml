interface:
  display_name: "Performance"
  short_description: "Use when something is measurably slow or hot paths need optimization with proof. Profiles first. For broad measured wins, use rigor:improve-measured."
  default_prompt: "Use code-ops-suite:performance for this task."
policy:
  allow_implicit_invocation: true
