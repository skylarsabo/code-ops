interface:
  display_name: "Handoff"
  short_description: "Use when a long run needs a continue, compact, or transfer decision. A transfer captures verifiable state as HANDOFF.md; resume re-verifies every claim."
  default_prompt: "Use code-ops-suite:handoff for this task."
policy:
  allow_implicit_invocation: true
