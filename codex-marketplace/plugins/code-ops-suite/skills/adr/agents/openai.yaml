interface:
  display_name: "ADR"
  short_description: "Use when you need to capture the reasons behind a codebase's architecture as decision records, either backfilling load-bearing past decisions or authoring an ADR for a current one."
  default_prompt: "Use code-ops-suite:adr for this task."
policy:
  allow_implicit_invocation: true
