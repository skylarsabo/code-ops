interface:
  display_name: "Vault"
  short_description: "Use when a repo needs its Obsidian docs vault created, an existing docs tree migrated into the standard layout, or an existing vault checked for conformance."
  default_prompt: "Use code-ops-suite:vault for this task."
policy:
  allow_implicit_invocation: true
