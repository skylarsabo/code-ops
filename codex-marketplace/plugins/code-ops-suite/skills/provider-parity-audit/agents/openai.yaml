interface:
  display_name: "Provider Parity Audit"
  short_description: "Use to audit the suite on Claude, Codex, Grok, and OpenCode for host-specific assumptions in hooks, agents, skills, scripts, settings, manifests, and docs."
  default_prompt: "Use code-ops-suite:provider-parity-audit for this task."
policy:
  allow_implicit_invocation: true
