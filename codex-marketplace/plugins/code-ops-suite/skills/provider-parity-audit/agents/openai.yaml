interface:
  display_name: "Provider Parity Audit"
  short_description: "Use when the marketplace must be audited across Claude, Codex, installed Grok, and OpenCode for host-specific assumptions in hooks, agents, skills, scripts, settings, manifests, generated projections, and documentation."
  default_prompt: "Use code-ops-suite:provider-parity-audit for this task."
policy:
  allow_implicit_invocation: true
