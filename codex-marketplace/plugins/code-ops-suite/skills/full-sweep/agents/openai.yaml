interface:
  display_name: "Full Sweep"
  short_description: "Use when you want the whole code-ops-suite run end-to-end on one codebase as a guided, checkpointed pipeline. Intra-plugin orchestrator; for the cross-plugin superset use everything."
  default_prompt: "Use code-ops-suite:full-sweep for this task."
policy:
  allow_implicit_invocation: false
