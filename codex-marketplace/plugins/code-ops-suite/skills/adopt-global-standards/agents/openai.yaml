interface:
  display_name: "Adopt Global Standards"
  short_description: "Use when the user's Claude or Codex global standards contract needs to be created, aligned to current doctrine, or re-verified after the marketplace moves."
  default_prompt: "Use code-ops-suite:adopt-global-standards for this task."
policy:
  allow_implicit_invocation: true
