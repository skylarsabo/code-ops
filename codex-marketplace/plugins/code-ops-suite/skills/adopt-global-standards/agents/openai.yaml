interface:
  display_name: "Adopt Global Standards"
  short_description: "Use when the user's global ~/.claude/CLAUDE.md needs to be created, brought up to the suite's current doctrine, or re-verified after the marketplace's standards moved."
  default_prompt: "Use code-ops-suite:adopt-global-standards for this task."
policy:
  allow_implicit_invocation: true
