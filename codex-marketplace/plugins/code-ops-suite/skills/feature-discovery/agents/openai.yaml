interface:
  display_name: "Feature Discovery"
  short_description: "Use when you want grounded, high-value feature ideas mined from the codebase (not a generic wishlist). Discovery only — writes no code."
  default_prompt: "Use code-ops-suite:feature-discovery for this task."
policy:
  allow_implicit_invocation: false
