interface:
  display_name: "Atlas"
  short_description: "Use to create a repo's atlas, its durable cache of codebase judgment, refresh it after code moves, or consolidate inbox notes. atlas-check sets freshness."
  default_prompt: "Use code-ops-suite:atlas for this task."
policy:
  allow_implicit_invocation: true
