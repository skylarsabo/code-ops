interface:
  display_name: "Remediation"
  short_description: "Use when a FINDINGS_REGISTER.md already exists and you want its NEEDS-REVIEW and NEEDS-DESIGN items implemented safely with tests. It requires a register as input."
  default_prompt: "Use code-ops-suite:remediation for this task."
policy:
  allow_implicit_invocation: true
