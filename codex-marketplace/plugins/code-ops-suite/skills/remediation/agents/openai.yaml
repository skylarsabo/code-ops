interface:
  display_name: "Remediation"
  short_description: "Use when a FINDINGS_REGISTER.md exists and its NEEDS-REVIEW and NEEDS-DESIGN items need safe implementation with tests. Requires a register as input."
  default_prompt: "Use code-ops-suite:remediation for this task."
policy:
  allow_implicit_invocation: true
