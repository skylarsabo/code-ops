interface:
  display_name: "Run Cost Audit"
  short_description: "Use to audit a finished run's cost discipline: dispatch counts, artifact sizes, and tier and effort mix. Requires its artifact folder, not a live run."
  default_prompt: "Use code-ops-suite:run-cost-audit for this task."
policy:
  allow_implicit_invocation: true
