interface:
  display_name: "Codebase Audit"
  short_description: "Use when you want a broad, multi-lens review of an unfamiliar or drifting codebase. It writes a ranked findings backlog and applies only safe fixes."
  default_prompt: "Use code-ops-suite:codebase-audit for this task."
policy:
  allow_implicit_invocation: true
