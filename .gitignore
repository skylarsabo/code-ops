# OS / editor cruft
.DS_Store
Thumbs.db
.vscode/
.idea/

# Claude Code install-time markers (created in the plugin cache, never part of source)
.in_use/

# node (none today, but defensive)
node_modules/


docs/specs
docs/superpowers
# local audit-run artifacts (findings registers, summaries) — never tracked
docs/code-ops-run/

# local run artifacts in the vault — never tracked (ADR 0001)
code-ops-docs/80 Runs/
# machine-specific Obsidian vault state (the standard's Git section)
code-ops-docs/.obsidian/workspace.json
code-ops-docs/.obsidian/workspace-mobile.json
code-ops-docs/.obsidian/cache
code-ops-docs/.trash/

# third-party specifications kept locally for reference — licensed, never redistributed
docs/*.pdf